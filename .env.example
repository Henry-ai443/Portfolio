# Optional: API base URL (leave empty to use Vite proxy in dev)
# VITE_API_URL=http://localhost:5000

# Admin login is handled by the API (signup once at /admin, then login with email + password).
# No frontend env needed for admin auth.
